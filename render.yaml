services:
  - type: web
    name: paytoday-namibian-loans
    env: static
    buildCommand: cd app && npm install && npm run build
    publishDir: app/dist
    envVars:
      - key: NODE_VERSION
        value: "18"
